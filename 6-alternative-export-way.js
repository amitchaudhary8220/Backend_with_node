module.exports.items = ["itme1", "item2"];

const person = { name: "chhotu" };

module.exports.singlePerson = person;
//why we can add any name after exports, because exports is an object,so we are just adding key inside the module with that data